/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["github.com", "funroad.store", "images.unsplash.com"], // 👈 Add your external image domain here
  },
};

export default nextConfig;
